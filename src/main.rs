fn main() {
    eprintln!("This crate provides multiple binaries:");
    eprintln!("  - tcplister: TCP listener server");
    eprintln!("  - client: TCP client");
    eprintln!();
    eprintln!("Usage: cargo run --bin [tcplister|client]");
}
